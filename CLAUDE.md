# PLOVE — 작업 규칙

사랑을 실천으로 연습하는 서비스. 매일 하나씩 현실에서 실천하고, 인증과 회고 대화로 검증받아 성장한다.
**해커톤 제출용**이다. 2026-09-20 시작.

---

## 1. 정본이 어디인가

| 무엇 | 정본 |
| --- | --- |
| 화면 동작·간격·모션 | **`handoff/prototype/Plove 웹 01-02.dc.html`** — 동작하는 화면 자체가 명세다 |
| 규칙·수치·API·DB | `handoff/docs/상세 기획서.html` (19섹션) |
| 화면별 정책 | `handoff/docs/화면 정책.md` |
| 미션 내용 | `handoff/missions.json` (66개) |
| 경제·토큰·문구·화면 목록 | `handoff/economy.json` · `design-tokens.json` · `copy.ko.json` · `screens.json` |
| 개발 순서 | `docs/개발-계획.md` |

**문서끼리 어긋나는 곳이 7군데 있다.** `docs/개발-계획.md` §1 에 표로 정리했다 — 판단이 필요하면 거기부터 본다.
⚠️ 그중 **`economy.json` 의 `ranking` 은 틀렸다**(XP 1순위로 적혀 있으나 기획서·화면정책 모두 LV 1순위). 그대로 읽으면 조용히 잘못된 랭킹이 나온다.

## 2. 실측 (추측 아님)

- 미션 **66개** (6코스 × 11) · 11번이 코스 회고(`isFinale`)
- 인증은 **전부 사진**이다. 영상·음성·그림 경로는 명세에만 있고 데이터에 없다
- 프로토타입 **5,653줄** · 화면 23개 · `sc-if` 분기 29개
- LLM 호출 **5곳** — `planTurns` `askNpc` `gradeMission` `writeReading` `writeLetter`
- 그 5곳이 전부 **`window.claude.complete`** 를 부른다. **배포 환경에 없는 API다 — 전부 갈아야 한다**
- 에셋 67장 **32MB**, 1MB 넘는 것 13장

## 3. 기술 스택

newb 방식 그대로. Next.js(App Router) + TypeScript · **상태는 React 기본만**(외부 상태 라이브러리 금지) · Supabase(Postgres + Google OAuth + RLS + Storage) · Vercel · **LLM 은 Gemini**(`@google/genai`).

- **결제는 붙이지 않는다.** 목업 유지 — 확인창 → 로딩 1.5초 → 완료 토스트 3단계 UI 는 그대로 산다
- **이메일은 보류.** 도메인이 없어 보내도 스팸으로 분류된다. 파트너 초대·선물은 화면까지만
- 스타일은 **프로토타입의 인라인 스타일을 그대로 가져온다.** Tailwind 는 스택 유지용으로 설치만 하고 화면 코드에 쓰지 않는다
- ⚠️ `LLM 비용.md` 의 단가표는 **Claude 기준**이다. Gemini 로 가면 그대로 못 쓴다 — 2단계에서 실제 토큰을 재서 다시 쓴다

## 4. 링크가 먼저 산다

**완성하고 배포하지 않는다. 빈 화면이라도 먼저 배포하고 그 위에 얹는다.**
`main` push = Vercel 자동 배포. 단계마다 배포된 링크가 자란다.

## 5. 반드시 지킬 것 (어기면 조용히 깨진다)

- **한글 입력** — 조합 중 Enter 를 막는다. `isComposing` + `keyCode 229` + `compositionstart/end` 를 **함께** 본다. 안 하면 "안녕"이 "안"으로 전송된다
- **채팅 레이아웃** — 루트를 `position: fixed; inset: 0` 으로 둔다. `min-height: 100vh` 를 쓰면 대화가 길어질 때 입력창이 화면 밖으로 밀린다
- **보조 텍스트 색** — `#7a6360` 보다 연한 회색을 본문에 쓰지 않는다. 대비가 모자란다. 이 프로젝트에서 여러 번 반려된 규칙이다
- **미션 완료 대기** — 채점이 빨리 끝나도 **최소 7초**는 로딩을 유지한다. 세 단계를 보여준 뒤 결과가 터지는 연출이다
- **점수를 보여주지 않는다** — 결과에는 획득 XP·보석과 NPC 코멘트만. 숫자 평가는 연습의 동기를 해친다
- **가족 코스는 구성을 먼저 묻는다** — 없는 가족을 전제로 미션을 주면 상처가 된다. 고르지 않은 대상의 미션은 생성하지 않는다
- **토큰 치환** — `{m}`(가족) `{p}`(반려동물)을 제목·목표·과제·기록칸·슬롯 라벨 **전부**에 적용한다
- **LLM 이밸의 `heavy` 케이스** — 감정적으로 무너진 사용자에게 "더 해보세요"가 나가면 배포하지 않는다. 다른 지표가 아무리 좋아도 중단이다

## 6. 작업 원칙

- **정책·수치가 코드와 어긋나면 기획서와 프로토타입이 정본이다.** 임의로 바꾸지 말고 확인한다
- **요청한 부분만 수정한다.** 요청하지 않은 레이아웃·간격·폰트·색은 건드리지 않는다
- 근거 없는 수치·성과를 쓰지 않는다
- 사용자가 준 문구는 그대로 쓴다
- 문구 톤은 **해요체**. 실패를 사용자 잘못으로 돌리지 않는다
- **작업·결정을 남긴다** — 의미 있는 작업은 §7 에, 기술·정책 결정은 §8 에 각각 한 줄씩(날짜 포함)

## 7. 진행 로그

- [x] 2026-09-20 프로젝트 생성 — 빈 뼈대
- [x] 2026-09-20 인수인계 수령·정독 — 문서 4종·데이터 5종·프로토타입 실행 확인(콘솔 에러 0). 실측치와 문서 충돌 7건을 `docs/개발-계획.md` 에 정리
- [x] 2026-09-20 계획 승인 — 마감 오늘 23:00. **React 이식은 버리고 프로토타입을 그대로 배포**하는 길로 전환(디자인이 달라질 여지가 없고 가장 빠르다). 게스트 모드는 넣지 않는다
- [x] 2026-09-20 0단계 — 프로토타입을 repo 루트로 올리고 `head` 에 title·OG 주입 · GitHub public `INKIM/plove` 생성·push
- [x] 2026-09-20 1단계 Gemini — `api/llm.js` 한 라우트 + `window.claude` shim. **화면 코드 5곳은 한 줄도 안 고쳤다.** 브라우저 실호출 1.9초 확인
- [x] 2026-09-20 2단계 완주 검증 — 로컬에서 미션 한 판 완주(사진·기록→회고 대화→채점→결과→연속 축하). NPC 가 사용자 말을 인용하고 `followUp` 을 따라간다. XP 120(100×1.2)·보석 2·코멘트 숫자 없음
- [x] 2026-09-20 에셋 **32MB → 6MB** — 렌더 px 실측 기준 축소 + 미참조 5장 제거. 운영 반영 확인
- [x] 2026-09-20 모바일(375×812) 확인 — 홈 하단 탭·수행 세로 스택·채팅 상하 고정 정상
- [x] 2026-09-20 **진행 저장**(`persist.js`) — 새로고침해도 LV·XP·보석·스트릭·기록이 남는다. 화면 코드 무수정
- [x] 2026-09-20 **다시 보기 결함 3건 교정**(프로토타입 기존 결함) — `[object Object]` · 사진 안 보임 · 보상 +0
- [x] 2026-09-20 **구글 로그인**(`auth.js`, Supabase OAuth) — `renderVals` 를 감싸 `googleStart`·`googleSignIn`·`doWipe` 만 교체. 설정이 없으면 무동작(프로토타입 가짜 로그인 유지). 세션 없으면 진행이 있어도 시작 화면에서 막는다
- [x] 2026-09-20 **단계 아이콘 22장 켬** — `stageIcon()` 첫 줄의 죽은 `return "";` 제거. 슬러그 22개 파일 전부 있음, 노드 11개 + 사이드바에 적용 확인
- [ ] ▶ **운영 env 3개 등록 + Supabase 리다이렉트 허용** (사용자 작업)

## 8. 개발 중 결정 (ADR)

- 2026-09-20 데스크톱 `PLove` 폴더(이미지 20장)는 쓰지 않는다 — 인수인계의 `assets/` 67장이 정본
- 2026-09-20 **스타일은 인라인 그대로 가져온다** — 프로토타입이 DC 템플릿이라 CSS 클래스가 없다. Tailwind 로 번역하면 5,653줄을 다시 쓰는 일이고 틀어져도 대조 기준이 프로토타입 화면뿐이다
- 2026-09-20 **프로토타입을 React 로 옮기지 않고 그대로 배포한다** — 마감이 5시간이다. DC 런타임(`support.js` + React UMD + Babel standalone)이 브라우저에서 그대로 돌고, 이 방식이면 디자인이 0% 달라진다. 포트폴리오를 배포한 그 방식이다
- 2026-09-20 **LLM 통로는 `api/llm.js` 하나다** — 프로토타입이 부르던 `window.claude.complete` 를 shim 이 그대로 받아 이 라우트로 넘긴다. 호출부 5곳을 건드리지 않으므로 프로토타입과 배포본이 갈라지지 않는다. 실패하면 shim 이 **던진다** — 호출부마다 폴백 문구가 이미 있어서 던져야 그것이 산다
- 2026-09-20 **모델은 `gemini-3-flash-preview`** — 같은 키로 `gemini-2.5-flash` 는 404 다("no longer available to new users"). newb 와 같은 결론
- 2026-09-20 **⚠️ Supabase env 는 리다이렉트 URL 을 허용목록에 넣은 뒤에 등록한다** — 순서가 바뀌면 로그인 시 newb 의 Site URL 로 튕긴다. 설정이 아예 없으면 `auth.js` 가 무동작이라 앱은 멀쩡하다(그게 더 안전한 상태다)
- 2026-09-20 **저장은 컴포넌트를 바깥에서 감싼다**(`persist.js`) — DC 런타임이 마운트한 뒤 `StreamableComponent.logic` 을 찾아 `setState` 를 감싼다. 화면 코드를 한 줄도 안 고치므로 프로토타입과 갈라지지 않는다. ⚠️ **사진은 `blob:` URL 이라 저장에서 뺀다** — 남기면 새로고침 뒤 깨진 이미지가 뜬다. 실제 보존은 스토리지가 붙어야 한다
- 2026-09-20 **다시 보기 결함 3건은 고친다** — 디자인 변경이 아니라 데이터를 잘못 읽던 것이다(`reviewLines` 가 `{hint,value}` 를 통째로 text 에 넣어 `[object Object]` · `reviewShots` 가 `photos` 를 `shots` 로 읽음 · 기록에 xp/gems 를 안 남겨 늘 +0). 같은 값을 코스 회고는 바르게 쓰고 있었다
- 2026-09-20 **게스트 모드를 넣지 않는다**(사용자 결정) — 구글 로그인이 있어야 들어온다. ⚠️ 그래서 2단계 로그인이 선택이 아니라 필수가 됐다
- 2026-09-20 **LLM 을 저장보다 먼저 붙인다** — 기획서 19장은 OAuth·스토리지를 1차로 두었으나, 대화가 없으면 데모가 성립하지 않는다. 계정이 없어도 한 판은 돈다
