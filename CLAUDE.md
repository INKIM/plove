# 사랑 학습앱 — 작업 규칙

> 2026-09-20 시작. **아직 기획서·인수인계 문서를 받기 전이라 이 문서는 뼈대다.**
> 문서가 도착하면 그것을 정본으로 삼아 §1·§3 을 다시 쓴다.

---

## 1. 지금까지 정해진 것

| 항목 | 내용 |
| --- | --- |
| 무엇을 만드나 | **연애·관계 기술 학습앱** — 대화법·갈등 해결·애착 유형 같은 실천적 관계 역량을 배우는 앱 |
| 기술 스택 | **미정.** 사용자가 보낼 인수인계 파일·기획서를 보고 정한다 — **임의로 고르지 않는다** |
| 데스크톱 `PLove` 폴더 | **무관하다.** 큐피드·캐릭터 이미지 22장이 들어 있으나 이 프로젝트와 잇지 않기로 했다(사용자 결정) |
| 폴더 이름 | 임시다. 제품명이 정해지면 바꾼다(아직 원격 저장소가 없어 지금은 이름만 바꾸면 된다) |

## 2. 받을 것 — 도착 전까지 코드에 착수하지 않는다

- [ ] **인수인계 파일** → `docs/` 에 원본 그대로 둔다
- [ ] **기획서** → `docs/` 에 원본 그대로 둔다

도착하면 순서는 이렇다: ① 전부 읽는다 ② 이 문서의 §1·§3 을 문서 기준으로 다시 쓴다 ③ **그다음에** 스캐폴드.
순서를 건너뛰고 먼저 만들어 두면, 문서가 다른 구조를 요구할 때 지우는 데 시간이 두 배로 든다.

## 3. 작업 원칙

- **정책·수치가 코드와 어긋나면 기획서가 정본이다.** 임의로 바꾸지 말고 사용자에게 확인한다
- **요청한 부분만 수정한다.** 요청하지 않은 레이아웃·간격·폰트·색은 건드리지 않는다
- 근거 없는 수치·성과를 쓰지 않는다
- 사용자가 준 문구는 그대로 쓴다. 문단 안의 줄바꿈은 의도한 것으로 본다
- **작업·결정을 남긴다** — 의미 있는 작업을 끝내면 §4 에, 기술·정책 결정을 내리면 §5 에 각각 한 줄씩(날짜 포함) 추가한다. 로그 갱신은 그 작업의 일부다

## 4. 진행 로그

> 규칙: 의미 있는 작업을 끝낼 때마다 한 줄씩(`- [x] YYYY-MM-DD 내용`). 다음 할 일은 `- [ ]`, 현재 착수 대상은 `▶`.

- [x] 2026-09-20 프로젝트 생성 — 빈 뼈대(`docs/` · CLAUDE.md · .gitignore · git init). 코드 없음
- [ ] ▶ **인수인계 파일·기획서 수령 → 정독 → 이 문서 갱신 → 스택 결정**

## 5. 개발 중 결정 (ADR)

> 규칙: 기술·정책 결정을 내릴 때마다 한 줄씩(`- YYYY-MM-DD 결정 내용`).

- 2026-09-20 데스크톱 `PLove` 에셋은 쓰지 않는다 — 완전히 새 프로젝트로 간다(사용자 결정)
- 2026-09-20 **기술 스택은 기획서를 본 뒤에 정한다.** 먼저 스캐폴드를 만들어 두지 않는다 — 문서가 다른 구조를 요구하면 그것을 걷어내는 일이 먼저가 된다
